// TypeScript
// npm install -g typescript
// tsc --init

// Plugiin Code Runner
// npm i -g ts-node
// ctrl + alt + n 

console.log("nmp install -g typescript");
const pedido = (pedido: string) => {
    return `Deu bom o seu pedido ${pedido}`
};

console.log(pedido("sapato"));